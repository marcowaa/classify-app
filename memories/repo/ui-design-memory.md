- Source of truth: docs/BRAND_VISUAL_SYSTEM.md
- Parent/auth visual language: deep navy background + cyan/teal CTA gradients.
- Child-trial cues: violet accents only as secondary branch (not global replacement).
- Public funnel pages must expose crawlable legal/policy links.
- Page design must start with audience intent: parent trust / child engagement / admin utility.
- Always verify RTL + mobile CTA visibility before finalizing UI.
- First-entry flow files: `client/src/pages/AgeGate.tsx` and `client/src/pages/Home.tsx` should keep a shared visual language (navy/cyan base, teal trust accents, violet as selective child-action accent).
- Motion baseline for first-entry screens: lightweight reveal transition via `requestAnimationFrame` + staggered opacity/translate styles (avoid heavy animation libraries).
- Mobile safety rule on AgeGate: fixed bottom CTA requires extra card bottom spacing (`pb-20` class pattern) to prevent overlap with content.
- Validation baseline after first-entry polish (2026-04-04): `npx tsc --noEmit` pass, `npm run build` pass, `npm run test -- --runInBand` pass, `/api/health` returns 503 degraded only when local DB at `127.0.0.1:5434` is unavailable.
- First-entry accessibility baseline (2026-04-04 continuation): Home and AgeGate reveal animations must respect `prefers-reduced-motion` and switch to near-static transitions when reduction is requested.
- Mobile header behavior baseline: when Home mobile action menu is open, lock document body scroll to prevent background scrolling under the menu.
- Page-by-page progression note (2026-04-04): `/download` (`client/src/pages/DownloadApp.tsx`) updated with first-party UX baseline: reduced-motion-friendly reveals, stronger focus-visible states on all key CTAs, and mobile-safe download CTA layout (`w-full` on small screens to avoid overflow).
- Page-by-page progression note (2026-04-04): `/parent-auth` (`client/src/pages/ParentAuth.tsx`) updated with accessibility-first polish: reduced-motion-aware ambient behavior, scroll lock while mobile top-actions menu is open, consistent focus-visible rings on auth actions/links, improved ARIA semantics for role/menu and segmented email/phone toggle, and cleaner quick-links presentation without decorative emoji noise.
- Page-by-page progression note (2026-04-04): `/child-link` (`client/src/pages/ChildLink.tsx`) updated with modal and form accessibility baseline: quick-actions + camera overlays now expose dialog semantics and Escape close behavior, quick-actions trigger/close focus handling improved, remove-child control now remains keyboard-discoverable, name/code inputs wired with `aria-invalid` + `aria-describedby` to live error regions, and key CTA scale effects/decorative motion now respect `prefers-reduced-motion`.
- Program governance baseline (2026-04-04): master coordination plan adopted in `docs/UI_UX_SEO_ASO_AGENT_COORDINATION_PLAN_2026-04.md`, execution activated on branch `sprint/2026-04-ui-seo-aso`, and Phase 0 baseline gate captured in `docs/UI_UX_SEO_ASO_PHASE0_EXECUTION_STATUS_2026-04-04.md` with TS/build/tests/health passing.
- SEO boundary baseline (2026-04-04 continuation): `client/src/hooks/useSEO.tsx` now enforces an explicit indexable-route whitelist; all other routes emit `noindex, nofollow, noarchive` for robots/googlebot/bingbot, with canonical path normalization (strip query/hash + alias mapping). Sitemap and llms public-page list were aligned by removing auth route from indexable set.
- SEO title authority fix (2026-04-04): `client/src/App.tsx` branding hook no longer writes `document.title`; route-level titles from `useSEO` are now preserved, restoring per-route SEO titles in runtime navigation.
- ASO parity baseline (2026-04-04): keep a single ASO payload in `client/public/apps/release-content.json` and inject it into generated `latest-release.json` via both publish scripts; consumers (`Home.tsx`, `DownloadApp.tsx`) should read release metadata first so copy keys, screenshots, and artifact links stay synchronized.
- JSX safety baseline (2026-04-18): during page-by-page visual rewrites, replace one full `return (...)` block in a single patch (avoid partial nested merges) to prevent mismatched closing tags; run `npx tsc --noEmit` immediately after patch before build/test.
