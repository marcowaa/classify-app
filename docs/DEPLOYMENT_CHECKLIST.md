# Deployment Checklist — Pre-Production Requirements\n<!-- synced: copilot-instructions.md on 2026-03-19 16:00 UTC -->\n\n**Purpose**: Verify all systems ready before deployment  \n**Authority**: `.github/copilot-instructions.md` → \"FINAL COMPLETION CHECKLIST\"  \n**Status**: 🟢 SYNCHRONIZED  \n**Version**: 1.0  \n**Last Updated**: 2026-03-19 16:00 UTC\n\n---\n\n## ✅ PRE-DEPLOYMENT VERIFICATION (MANDATORY)\n\n### Phase 1: Code Quality (MUST ALL PASS)\n\n```bash\n# 1. TypeScript compilation check\n[ ] npx tsc --noEmit\n    └─ Exit code must be 0\n    └─ Pre-existing errors in child.ts/parent.ts allowed\n    └─ NO new errors\n\n# 2. Production build verification\n[ ] npm run build\n    └─ Must complete without errors\n    └─ Frontend output: dist/public/\n    └─ Backend output: dist/index.js\n    └─ ESM format, Node platform\n\n# 3. Unit test execution\n[ ] npm run test -- --runInBand\n    └─ ALL tests must pass\n    └─ NO skipped tests\n    └─ Coverage > 80% in critical paths\n\n# 4. Health check endpoint\n[ ] curl http://localhost:5000/api/health\n    └─ Must return: {\"status\":\"ok\"}\n    └─ HTTP 200 status code\n    └─ No errors in server logs\n```\n\n**Failure Rule**: If ANY check fails → DO NOT DEPLOY\n\n---\n\n### Phase 2: Application Startup\n\n```bash\n[ ] App boots successfully\n    └─ Command: node dist/index.js\n    └─ No errors in stderr\n    └─ Ready message in logs\n    └─ Port 5000 listening (or env PORT)\n\n[ ] No missing dependencies\n    └─ Check: npm list (no missing/peer warnings)\n    └─ All node_modules present\n    └─ Lockfile up to date\n\n[ ] Database connection established\n    └─ Drizzle ORM connects\n    └─ Migrations applied: npm run db:push\n    └─ No schema validation errors\n\n[ ] Environment variables configured\n    └─ .env has all required keys\n    └─ NODE_ENV=production\n    └─ JWT_SECRET set\n    └─ SMTP credentials valid\n    └─ Database URL valid\n```\n\n---\n\n### Phase 3: Asset Verification\n\n```bash\n[ ] Frontend assets served correctly\n    └─ GET /index.html → 200 OK\n    └─ GET /assets/* → 200 OK (all CSS/JS/PNG files)\n    └─ GET /sw.js → 200 OK (service worker)\n    └─ GET /manifest.json → 200 OK (PWA manifest)\n    └─ GET /favicon.ico → 200 OK\n\n[ ] Static file paths correct\n    └─ Directory: dist/public/\n    └─ Middleware: app.use(express.static(...))\n    └─ No 404s on common files\n\n[ ] SPA fallback working\n    └─ GET /unknown-route → serves index.html (not 404)\n    └─ BUT: /api/* returns 404 (not SPA fallback)\n    └─ BUT: /assets/* returns 404 if missing (not fallback)\n    └─ BUT: /sw.js returns 404 if missing (not fallback)\n```\n\n---\n\n### Phase 4: API Contract Verification\n\n```bash\n[ ] Success response format correct\n    └─ { \"success\": true, \"data\": {...}, \"message\": \"...\" }\n    └─ MUST have these 3 fields\n    └─ No extra fields in root\n\n[ ] Error response format correct\n    └─ { \"success\": false, \"error\": \"CODE\", \"message\": \"text\" }\n    └─ Error code is standard (UNAUTHORIZED, NOT_FOUND, etc.)\n    └─ Message is human-readable\n\n[ ] Sample endpoints return correct format\n    └─ POST /api/auth/login → Follows contract\n    └─ GET /api/parent/profile-data → Follows contract\n    └─ GET /api/health → { \"status\": \"ok\" }\n```\n\n---\n\n### Phase 5: Security Compliance\n\n```bash\n[ ] Authentication required on protected endpoints\n    └─ Missing token → 401 UNAUTHORIZED\n    └─ Invalid token → 401 UNAUTHORIZED\n    └─ Expired token → 401 UNAUTHORIZED\n\n[ ] Parent-child ownership validated\n    └─ Parent can only access own children\n    └─ Accessing child of another parent → 403 FORBIDDEN\n    └─ Pattern: parentId === req.user.id check exists\n\n[ ] Rate limiting active\n    └─ /api/auth/login: 5/min limit enforced\n    └─ /api/auth/request-otp: 3/min limit enforced\n    └─ Exceed limit → 429 TOO_MANY_REQUESTS\n    └─ Response headers include X-RateLimit-*\n\n[ ] CORS configured safely\n    └─ Origin validation enabled (not '*')\n    └─ Credentials: true if needed\n    └─ Allowed methods: GET, POST, PUT, DELETE, OPTIONS\n    └─ No overly permissive headers\n\n[ ] HTTPS/SSL configured (if production)\n    └─ All external URLs use https://\n    └─ Redirect http → https\n    └─ HSTS header present\n    └─ SSL certificate valid (not self-signed)\n\n[ ] No secrets in logs\n    └─ Grep logs for JWT patterns → None found\n    └─ Grep logs for password → None found\n    └─ Grep logs for OTP codes → None found (prod only)\n    └─ Grep logs for API keys → None found\n```\n\n---\n\n### Phase 6: Functionality Testing\n\n```bash\n[ ] Authentication flow works\n    └─ Register new parent account\n    └─ Login with credentials\n    └─ Receive valid JWT token\n    └─ Token works for protected endpoints\n\n[ ] Family management works\n    └─ Add child to account\n    └─ Update child details\n    └─ List children\n    └─ Child data in correct format\n\n[ ] Notifications delivery (if enabled)\n    └─ Email sending configured\n    └─ SMTP credentials valid\n    └─ Sample email sends successfully\n    └─ Email contains correct data\n\n[ ] Database operations work\n    └─ Can create records\n    └─ Can read records\n    └─ Can update records\n    └─ Relationships maintained (foreign keys)\n    └─ Cascade deletes work\n\n[ ] Payment system ready (if applicable)\n    └─ Payment gateway credentials configured\n    └─ Test transaction succeeds\n    └─ Webhook handler receives events\n    └─ Payment status updates in database\n```\n\n---\n\n### Phase 7: Cleanup & Production State\n\n```bash\n[ ] No temporary files exist\n    └─ /docs/_ai-temp/ is empty or deleted\n    └─ No .tmp, .bak, .old files\n    └─ No commented-out code blocks\n\n[ ] No debug code remains\n    └─ Grep: No console.log() except structured logging\n    └─ No debugger statements\n    └─ No TODO comments in critical paths\n    └─ No development-only imports\n\n[ ] Git state is clean\n    └─ git status shows no uncommitted changes\n    └─ All changes committed\n    └─ Working directory matches HEAD\n\n[ ] Documentation updated\n    └─ if behavior changed → docs updated\n    └─ if API changed → API_ENDPOINTS_REFERENCE updated\n    └─ if security changed → SECURITY_AUDIT updated\n    └─ if rate limits changed → RATE_LIMITING_MATRIX updated\n    └─ Last Updated timestamps current\n\n[ ] Linked documentation synchronized\n    └─ copilot-instructions.md ↔ CRITICAL_FILES_BY_DOMAIN.md\n    └─ copilot-instructions.md ↔ SECURITY_AUDIT.md\n    └─ copilot-instructions.md ↔ API_ENDPOINTS_REFERENCE.md\n    └─ copilot-instructions.md ↔ RATE_LIMITING_MATRIX.md\n    └─ All sync audit comments added\n```\n\n---\n\n## 📋 Quick Checklist (One Per Line)\n\n```\nTypeScript check passed\nBuild succeeded\nAll tests passed\nHealth endpoint works\nApp boots without errors\nDatabase migrations applied\nEnvironment variables set\nFrontend assets serve correctly\nSPA fallback configured\nAPI response format verified\nAuthentication required on protected routes\nParent-child validation works\nRate limiting active\nCORS configured safely\nNo secrets in logs\nNo temp files\nNo debug code\nGit state clean\nDocumentation updated\nLinked docs synchronized\n```\n\n---\n\n## 🚨 Critical Failure Points\n\nDEPLOY IMMEDIATELY ABORTS IF:\n\n| Check | Failure Consequence | Resolution |\n|-------|-------------------|------------|\n| TypeScript fails | Cannot build | Fix all TS errors |\n| Build fails | No production artifacts | Fix build errors |\n| Tests fail | Regression possible | Fix failing tests |\n| Health check fails | Server not responding | Debug startup |\n| Missing env var | App crashes | Configure .env |\n| Secrets in logs | Security breach | Remove and redeploy |\n| Temp files present | Clean state violated | Delete temp files |\n| Parent-child validation missing | Security vulnerability | Implement check |\n| Rate limiting missing | Account takeover risk | Implement limits |\n\n---\n\n## 🔄 Post-Deployment Verification\n\nAfter deployment, verify:\n\n```bash\n# 1. Health endpoint responds\ncurl -i https://api.classify.com/api/health\n# Expected: 200 OK, { \"status\": \"ok\" }\n\n# 2. Frontend loads\ncurl -i https://classify.com/\n# Expected: 200 OK, HTML content\n\n# 3. API works\ncurl -i https://api.classify.com/api/auth/register \\\n  -X POST \\\n  -H \"Content-Type: application/json\" \\\n  -d '{...}'\n# Expected: 400-201 (valid response, not 500)\n\n# 4. Database accessible\n# Check logs: \"Connected to database\"\n\n# 5. Logs look normal\n# No ERROR or CRITICAL in deployment logs\n# Structure: timestamp, level, message, context\n```\n\n---\n\n## 📊 Deployment Sign-Off\n\n| Item | Responsible | Status | Date |\n|------|------------|--------|------|\n| Code quality checks | Build pipeline | [ ] | __ / __ |\n| Security audit | Security team | [ ] | __ / __ |\n| Performance validated | DevOps | [ ] | __ / __ |\n| Documentation complete | Engineering lead | [ ] | __ / __ |\n| Stakeholder approval | Product manager | [ ] | __ / __ |\n| **FINAL DEPLOYMENT APPROVAL** | **Tech lead** | **[ ]** | **__ / __** |\n\n---\n\n## 🔄 Sync Information\n\n**Source**: `.github/copilot-instructions.md` → \"FINAL COMPLETION CHECKLIST\"  \n**Related Documents**:\n- SECURITY_AUDIT.md (verify vulnerabilities fixed)\n- API_ENDPOINTS_REFERENCE.md (verify API contracts)\n- CRITICAL_FILES_BY_DOMAIN.md (verify critical files)\n\n**Sync Frequency**: Real-time (deployment process)  \n**Last Sync**: 2026-03-19 16:00 UTC\n\n**When adding new checks**:\n1. Add to appropriate phase (1-7)\n2. Include exact commands/rules\n3. Specify failure consequence\n4. Update timestamp\n\n---\n\n**Version**: 1.0  \n**Status**: 🟢 ACTIVE  \n**Failure Tolerance**: ZERO (any failed check = no deploy)  \n**Maintained By**: Classify Engineering  \n**Next Review**: 2026-04-19 (30 days)
